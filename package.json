{
  "name": "personalwebsite-webmcp-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright and WebMCP contract tests for an agent-friendly personal website.",
  "type": "module",
  "scripts": {
    "test": "node --test --test-isolation=none test/unit/*.test.mjs",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  },
  "engines": {
    "node": ">=24"
  }
}
